<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarEexample1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">タスク作成アプリ</a>
    </div>
      <ul class="nav navbar-nav">
        <li><%= link_to "タスク登録", new_task_path %></li>
        <li><%= link_to "タスク一覧", tasks_path %></li>
        <li><a href="#">メニューＣ</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li><%= link_to "Profile", user_path(current_user.id) %></li>
          <li><%= link_to "Log out ", session_path(current_user.id), method: :delete %></li>
          <%# binding.pry %>
          <% if current_user.admin? %>
            <li><%= link_to "管理者用ユーザー一覧", admin_users_path %></li>
            <li><%= link_to "管理者ユーザー作成", new_admin_user_path %></li>
          <% end %>
        <% else %>
          <li><%= link_to "Sign up", new_user_path %></li>
          <li><%= link_to "Log in", new_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
